# Local environment (never commit credentials)
.env

# Instagram import tooling (scripts/instagram_import.py): local venv and the
# logged-in session — both carry credentials, never commit.
scripts/.venv/
scripts/.instagram-session.json

# Local previews written by cmd/mailtest
email-*.html

# Build artifacts
/bin/
*.exe
*.test
*.out

# Editor/OS noise
.DS_Store
Thumbs.db

# Operator-only, kept out of the public repo
docs/
scripts/
tmp_*/
